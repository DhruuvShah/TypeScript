// Write a program to use array destructuring to assign values from an array to variables

const colors = ["red", "green", "blue"];
const [primary, secondary, tertiary] = colors;

console.log(primary, secondary, tertiary);
