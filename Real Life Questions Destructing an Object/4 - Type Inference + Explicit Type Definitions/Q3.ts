// An HR system automatically detects whether a value is text or number and stores employee
// data accordingly. Create a program showing both automatic and manually-defined data types.
// (→ Type inference + explicit type)

// Inferred
let empId = 101;            // number
let empFullName = "Amit";   // string

// Explicit
let isActive: boolean = true;
let designation: string = "Manager";

console.log(empId, empFullName, isActive, designation);

